
{% extends "base.html" %}

{% block title %}Proof It's Real - Temporal Monitoring{% endblock %}

{% block content %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4">
                <i class="fas fa-shield-alt me-2"></i>
                Proof It's Real - Network Provenance
            </h1>
            <p class="text-muted mb-4">
                Demonstrate real-time network connectivity and data pipeline integrity
            </p>
        </div>
    </div>

    <!-- Main Action Cards -->
    <div class="row mb-4">
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-satellite fa-2x text-primary mb-3"></i>
                    <h5 class="card-title">Ping BIPM</h5>
                    <p class="card-text">Test connectivity to BIPM timing servers</p>
                    <button class="btn btn-primary" onclick="pingHost('https://webtai.bipm.org/ftp/pub/tai/other-products/utcrlab/')">
                        <i class="fas fa-ping-pong-paddle-ball me-1"></i> Ping
                    </button>
                </div>
            </div>
        </div>

        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-globe fa-2x text-info mb-3"></i>
                    <h5 class="card-title">Ping IERS</h5>
                    <p class="card-text">Test connectivity to IERS data center</p>
                    <button class="btn btn-info" onclick="pingHost('https://datacenter.iers.org/data/9/dut1.txt')">
                        <i class="fas fa-ping-pong-paddle-ball me-1"></i> Ping
                    </button>
                </div>
            </div>
        </div>

        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-download fa-2x text-success mb-3"></i>
                    <h5 class="card-title">Pull Now</h5>
                    <p class="card-text">Trigger data download with provenance</p>
                    <button class="btn btn-success" onclick="pullData()">
                        <i class="fas fa-cloud-download-alt me-1"></i> Pull
                    </button>
                </div>
            </div>
        </div>

        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-database fa-2x text-warning mb-3"></i>
                    <h5 class="card-title">Provenance</h5>
                    <p class="card-text">View file hashes and download metadata</p>
                    <button class="btn btn-warning" onclick="loadProvenance()">
                        <i class="fas fa-fingerprint me-1"></i> View
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Analysis Cards -->
    <div class="row mb-4">
        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-chart-line fa-2x text-primary mb-3"></i>
                    <h5 class="card-title">Current Forecast</h5>
                    <p class="card-text">Latest GTI prediction analysis</p>
                    <button class="btn btn-outline-primary" onclick="loadForecast()">
                        <i class="fas fa-crystal-ball me-1"></i> View
                    </button>
                </div>
            </div>
        </div>

        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-history fa-2x text-info mb-3"></i>
                    <h5 class="card-title">10-yr Backtest</h5>
                    <p class="card-text">Historical forecast validation</p>
                    <button class="btn btn-outline-info" onclick="loadForecastHistory()">
                        <i class="fas fa-clock-rotate-left me-1"></i> View
                    </button>
                </div>
            </div>
        </div>

        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-bug fa-2x text-danger mb-3"></i>
                    <h5 class="card-title">Last Traceback</h5>
                    <p class="card-text">Recent system errors and exceptions</p>
                    <button class="btn btn-outline-danger" onclick="loadTraceback()">
                        <i class="fas fa-exclamation-triangle me-1"></i> View
                    </button>
                </div>
            </div>
        </div>

        <div class="col-lg-3 col-md-6 mb-3">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-file-alt fa-2x text-secondary mb-3"></i>
                    <h5 class="card-title">Tail Logs</h5>
                    <p class="card-text">Recent application log entries</p>
                    <button class="btn btn-outline-secondary" onclick="loadLogs()">
                        <i class="fas fa-scroll me-1"></i> View
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modals -->
<dialog id="m_ping" class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">Ping Results</h5>
            <button type="button" class="btn-close" onclick="closeModal('m_ping')"></button>
        </div>
        <div class="modal-body">
            <pre id="ping-results"></pre>
        </div>
    </div>
</dialog>

<dialog id="m_prov" class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">Data Provenance</h5>
            <button type="button" class="btn-close" onclick="closeModal('m_prov')"></button>
        </div>
        <div class="modal-body">
            <pre id="prov-results"></pre>
        </div>
    </div>
</dialog>

<dialog id="m_forecast" class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">Forecast Results</h5>
            <button type="button" class="btn-close" onclick="closeModal('m_forecast')"></button>
        </div>
        <div class="modal-body">
            <pre id="forecast-results"></pre>
        </div>
    </div>
</dialog>

<dialog id="m_logs" class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">Application Logs</h5>
            <button type="button" class="btn-close" onclick="closeModal('m_logs')"></button>
        </div>
        <div class="modal-body">
            <pre id="logs-results"></pre>
        </div>
    </div>
</dialog>

<script>
// Helper functions
async function fetchJson(path) {
    const response = await fetch(path);
    return await response.json();
}

function openModal(id) {
    document.getElementById(id).showModal();
}

function closeModal(id) {
    document.getElementById(id).close();
}

// Action functions
async function pingHost(url) {
    try {
        const result = await fetchJson(`/api/ping?url=${encodeURIComponent(url)}`);
        document.getElementById('ping-results').textContent = JSON.stringify(result, null, 2);
        openModal('m_ping');
    } catch (error) {
        document.getElementById('ping-results').textContent = `Error: ${error.message}`;
        openModal('m_ping');
    }
}

async function pullData() {
    try {
        const result = await fetchJson('/api/pull');
        document.getElementById('ping-results').textContent = JSON.stringify(result, null, 2);
        openModal('m_ping');
    } catch (error) {
        document.getElementById('ping-results').textContent = `Error: ${error.message}`;
        openModal('m_ping');
    }
}

async function loadProvenance() {
    try {
        const result = await fetchJson('/api/provenance?n=10');
        document.getElementById('prov-results').textContent = JSON.stringify(result, null, 2);
        openModal('m_prov');
    } catch (error) {
        document.getElementById('prov-results').textContent = `Error: ${error.message}`;
        openModal('m_prov');
    }
}

async function loadForecast() {
    try {
        const result = await fetchJson('/api/forecast');
        document.getElementById('forecast-results').textContent = JSON.stringify(result, null, 2);
        openModal('m_forecast');
    } catch (error) {
        document.getElementById('forecast-results').textContent = `Error: ${error.message}`;
        openModal('m_forecast');
    }
}

async function loadForecastHistory() {
    try {
        const result = await fetchJson('/api/forecast_history');
        document.getElementById('forecast-results').textContent = JSON.stringify(result, null, 2);
        openModal('m_forecast');
    } catch (error) {
        document.getElementById('forecast-results').textContent = `Error: ${error.message}`;
        openModal('m_forecast');
    }
}

async function loadTraceback() {
    try {
        const result = await fetchJson('/api/last_trace');
        document.getElementById('logs-results').textContent = JSON.stringify(result, null, 2);
        openModal('m_logs');
    } catch (error) {
        document.getElementById('logs-results').textContent = `Error: ${error.message}`;
        openModal('m_logs');
    }
}

async function loadLogs() {
    try {
        const result = await fetchJson('/api/logs?k=300');
        document.getElementById('logs-results').textContent = JSON.stringify(result, null, 2);
        openModal('m_logs');
    } catch (error) {
        document.getElementById('logs-results').textContent = `Error: ${error.message}`;
        openModal('m_logs');
    }
}

// Modal styling
document.addEventListener('DOMContentLoaded', function() {
    const dialogs = document.querySelectorAll('dialog');
    dialogs.forEach(dialog => {
        dialog.style.maxWidth = '80vw';
        dialog.style.maxHeight = '80vh';
        dialog.style.backgroundColor = 'var(--bs-dark)';
        dialog.style.color = 'var(--bs-light)';
        dialog.style.border = '1px solid var(--bs-secondary)';
        dialog.style.borderRadius = '0.375rem';
    });
});
</script>

<style>
dialog {
    padding: 0;
    border: none;
    border-radius: 0.375rem;
}

dialog::backdrop {
    background: rgba(0, 0, 0, 0.5);
}

.modal-content {
    background: var(--bs-dark);
    color: var(--bs-light);
    border: 1px solid var(--bs-secondary);
}

pre {
    background: var(--bs-gray-900);
    color: var(--bs-light);
    padding: 1rem;
    border-radius: 0.375rem;
    max-height: 400px;
    overflow-y: auto;
    font-size: 0.875rem;
}
</style>
{% endblock %}
